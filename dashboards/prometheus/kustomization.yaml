apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: grafana
generatorOptions:
  annotations:
    internal.hetzner.cloud/grafana-folder: Prometheus
  immutable: true
  labels:
    grafana_dashboard: ''
configMapGenerator:
- name: dashboard-prometheus
  files: [prometheus.json]
- name: dashboard-prometheus-remote-write
  files: [prometheus-remote-write.json]
